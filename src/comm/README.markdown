A simple procedural langauge with:

* integer arithmetic `+`, `-`, `*`, `/`
* boolean expresisons: `true`, `false`, `&&`, `||` and `!`
* varaibles
* conditional statements
* `while` loops

The language is compiled to simplified machine code with a stack.
