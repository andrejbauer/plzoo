These are sources for the Programming Languages Zoo available
at http://andrej.com/plzoo/. See the COPYRIGHT.txt file for
licensing information.

The top-level Makefile is used to publish the PL Zoo web page
and is therefore useful only to Andrej Bauer, who owns the web
page. Each subdirectory contains a programming language with its
own Makefile. Those are meant for public consumption.
